{
  "name": "micii-producatori-api",
  "version": "0.1.0",
  "scripts": {
    "format": "prettier --write .",
    "check:format": "prettier --check .",
    "start": "sls offline",
    "deploy": "sls deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.0.2",
    "serverless-offline": "^6.1.2",
    "serverless-plugin-typescript": "^1.1.9",
    "typescript": "^3.8.3"
  }
}
